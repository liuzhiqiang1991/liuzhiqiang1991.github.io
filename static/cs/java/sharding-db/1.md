### 准备工作
#### 1. 准备2个数据库
* docker run -d --name mysql-0 -p 3310:3306 -e TZ=Asia/Shanghai -e MYSQL_ROOT_PASSWORD=root mysql:5.7

* docker run -d --name mysql-1 -p 3311:3306 -e TZ=Asia/Shanghai -e MYSQL_ROOT_PASSWORD=root mysql:5.7

#### 2. 建库： demo-log
#### 3. 建表
<pre><code style="font-size: 17px; font-family: Microsoft YaHei; line-height: 30px;">
    CREATE TABLE `log` (
          `id` bigint(20) NOT NULL AUTO_INCREMENT,
          `product_id` bigint(20) NOT NULL,
          `content` varchar(255) DEFAULT NULL,
          PRIMARY KEY (`id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8
</code>
</pre>


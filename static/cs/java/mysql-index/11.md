### SQL提示
#### 1. use index
* 建议使用某个索引
* select * from t_user use index(索引名) where...
#### 2. ignore index
* select * from t_user ignore index(索引名) where...
#### 3. force index
* 强制使用某个索引
* select * from t_user force index(索引名) where...

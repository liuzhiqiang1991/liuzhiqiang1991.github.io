#### 插入数据
#### 1. insert优化
* 批量插入
* 手动提交事务
```
    start transaction;
    insert into ...
    insert into ...
    insert into ...
    commit;
```
* 主键顺序插入

#### 2. load指令
* mysql --local-infile -u root -p
* select @@local_infile;
* set global local_infile = 1;

* load data local infile '/tmp/文件名' into table 表名 fields terminated by ',' lines terminated by '\n';

<font color="#dd00dd" size=5>download</font><br />
#### 1. controller
```
    @GetMapping("download")
    public void download(HttpServletResponse response) throws IOException {
        List<String> columns = Arrays.asList(new String[] { "name", "age" });

        List<RecordDto> list = new ArrayList<>();
        for(int i = 1; i <= 5; i++) {
            RecordDto record = new RecordDto();
            record.setName("张三" + i);
            record.setAge(20 + i);
            list.add(record);
        }
        response.setContentType("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet");
        response.setCharacterEncoding("utf-8");
        String fileName = URLEncoder.encode("测试", "UTF-8").replaceAll("\\+", "%20");
        response.setHeader("Content-disposition", "attachment;filename*=utf-8''" + fileName + ".xlsx");
        EasyExcel.write(response.getOutputStream(), RecordDto.class)
                .includeColumnFiledNames(columns)
                .sheet("模板").doWrite(list);
    }
```

#### 2. 测试

<br />

### 快速入门
* Test Plan ——> 右键 ——> Add ——> Threads(Users) ——> Thread Group ——> 设置线程数（用户数）、Ramp-up period（用户访问时间）、Loop count（每个用户的访问次数）

* Thread Group ——> 右键 ——> Add ——> Sample ——> Http Request，然后填写相关信息，编码UTF-8
* Thread Group ——> 右键 ——> Add ——> Listener ——> View Results Tree (查看结果树)
* Thread Group ——> 右键 ——> Add ——> Listener ——> Summary Report (汇总报告)
* Thread Group ——> 右键 ——> start，然后点击结果数查看

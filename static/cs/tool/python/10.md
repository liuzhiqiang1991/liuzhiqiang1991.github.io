#### 1. 列表
<pre><code style="font-size: 18px; font-family: Microsoft YaHei; line-height: 30px">
    nums = [2, 5, 3, 0]
    nums.append(11)
    nums.remove(2)

    print(len(nums))
    print(nums[0])
    print(min(nums))
    print(max(nums))
    print(sorted(nums))
</code></pre>

#### 2. 字典
<pre><code style="font-size: 18px; font-family: Microsoft YaHei; line-height: 30px;">
    dict = {"Tom": "Tom green", "Jean": "Jean Gold"}
    dict["Louis"] = "Louis Green"
    dict["Jim"] = "Jim Gold"

    name = input("请输入名字")
    if name in dict:
        print("名字存在，全名：" + dict[name])
    else:
        print("名字不存在")
        print("当前字典词条数为：" + str(len(dict)))
</code></pre>

<br/>

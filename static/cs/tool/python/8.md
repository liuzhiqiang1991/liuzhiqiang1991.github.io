
<pre><code style="font-size: 18px; font-family: Microsoft YaHei; line-height: 28px">

    my_num = int(input("请输入一个数字"))
    if my_num >= 90:
        if my_num >= 95:
            print("A+")
        else:
            print("A")
    elif my_num >= 60:
        print("B")
    else:
        print("C")

    # 偏瘦：BMI <= 18.5
    # 正常：18.5 < BMI <= 25
    # 偏胖：25 < BMI <= 30
    # 肥胖：BMI > 30
    user_weight = float(input("请输入体重（单位：kg）"))
    user_height = float(input("请输入身高（单位：m)"))
    user_BMI = user_weight / user_height ** 2
    print("BMI: " + str(user_BMI))

    if user_BMI <= 18.5:
        print("偏瘦")
    elif 18.5 < user_BMI <= 25:
        print("正常")
    elif 25 < user_BMI <= 30:
        print("偏胖")
    else:
        print("肥胖")

</code></pre>

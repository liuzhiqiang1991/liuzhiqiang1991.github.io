### 线程隔离

* 在簇点链路中找到想要操作的接口

* 点击流控，在弹出的窗口中填写流控规则

* 并发线程数，单机阈值：5

<br/>

### 测试
#### 1. 修改配置，控制tomcat最大链接数(默认8192)
```
    server:
      tomcat:
        threads:
          max: 25
        accept-count: 25  ## 允许排队等待的数量
        max-connections: 100
```

#### 2. tomcat 默认参数值
```
    在SpringBoot2.7.10版本中内置Tomcat版本是9.0.73，SpringBoot内置Tomcat的默认设置如下：

    Tomcat的连接等待队列长度，默认是100
    Tomcat的最大连接数，默认是8192
    Tomcat的最小工作线程数，默认是10
    Tomcat的最大线程数，默认是200
    Tomcat的连接超时时间，默认是20s
```

#### 3. 通过jmeter访问http://localhost:8081/products接口
* number of threads(user): 5000
* Ramp-up period: 50
* Loop count: 1
* 平均每秒100次访问

#### 4. 同时访问 http://localhost:8081/hello 查看耗时
#### 5. 配置线程隔离，控制线程数为5，然后进行对比
<br/><br/><br/>

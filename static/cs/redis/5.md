### 字符串
```
    set name geekhour
    get name

    set Name geekHour
    get Name
    set age 25
    get age

    del name
    exists name
    keys *
    keys na*
    flushall

    redis-cli --raw
    quit
    clear

    ttl name
    expire name 10
    setex name 10 测试

```
